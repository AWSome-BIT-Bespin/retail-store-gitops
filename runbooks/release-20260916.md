# 2026-09-16 AWS Argo CD 배포 검증 기록

## 결과

2026-09-16 17:01 KST 기준, 기존 AWS 배포를 Argo CD에 연결하고 정식 이미지 5종으로 맞췄다. Application은 `Synced / Healthy`, Deployment 5개는 각각 2/2, Pod 10개는 Ready이며 재시작 0회다. 회원가입부터 주문 생성·재로그인 후 주문 조회까지 ALB의 실제 UI HTTP 폼을 통해 검증했다. 브라우저의 시각적 화면 품질 검사는 이 시험에 포함하지 않는다.

- 환경: AWS `350606136784`, `ap-northeast-2`, EKS `retail-dev-eks`, namespace/release `retail-dev`.
- Argo CD: v3.4.5, namespace `argocd`, Application `retail-dev`, project `default`.
- 배포 경로: `src/app/chart`, values 순서 `../../../environments/aws/values.yaml` → `versions.yaml`.
- 정책: 수동 sync, prune/force 비활성, HPA가 관리하는 replica 차이 제외.
- [GitOps PR #3](https://github.com/AWSome-BIT-Bespin/retail-store-gitops/pull/3).

## 배포 순서와 확인한 변경

1. 기존 Helm values와 실제 Secret **이름·키**, IRSA, DB/Valkey/DynamoDB 연결 설정, ALB, HPA, selector, Prometheus annotation을 대조했다. Secret 비밀값을 출력하거나 저장소에 넣지 않았다.
2. Catalog의 잘못된 WhaTap `valueFrom` 형식을 수정했다. Checkout은 명시적 serverHost 값과 기존 Secret fallback을 지원한다. Cart는 실제 table-name Secret 참조를 보존한다.
3. 준비 중 다른 작업자가 Catalog v0.0.3과 Checkout v0.0.4로 갱신했다. 실제 Deployment managedFields와 준비 상태를 확인해 그 변경을 그대로 인수했다.
4. 기준 커밋 `fa895c3dc1c1651102de24a91e4785d1b84eebe2`로 16:56:11 KST에 첫 수동 sync를 완료했다. 이 시점 버전은 Cart v0.0.1 / Catalog v0.0.3 / Checkout v0.0.4 / Orders v0.1.0 / UI v0.1.2다.
5. 릴리스 커밋 `4766e1a1dbef5dda55f662b55c8042ff42f242a3`에서 Argo diff가 **Cart·Orders·UI Deployment 세 개뿐**임을 확인하고 승격했다. sync operation은 16:57:03 KST에 완료됐고 이후 `Healthy`까지 확인했다.

기존 Bastion checkout에 있던 수정 파일 2개와 백업 파일은 수정하지 않았다. 이후 앱 배포는 GitOps 변경과 Argo CD 수동 sync로 관리한다. 기존 Helm 이력은 남아 있지만 같은 리소스에 별도의 Helm upgrade를 병행하지 않는다.

## 실행 중인 이미지 증거

ECR 접두사: `350606136784.dkr.ecr.ap-northeast-2.amazonaws.com/retail-`. 아래 digest는 ECR과 실제 Pod imageID가 일치했다.

| 서비스 | 태그 | SHA-256 digest |
|---|---|---|
| Cart | v0.0.2 | `d0b2f8e4ec97d5165851af43299691a70894a0668b244735abc631e512f2c0fc` |
| Catalog | v0.0.3 | `3de6e79eb9463e625cbca8b1d41cb438fee6f9dd0f23136c12a8ad174cdd384c` |
| Checkout | v0.0.4 | `b9e8321595338baee1971d1fc6da11f56c57f3d9d41d56e042cd3f0b599be045` |
| Orders | v0.1.1 | `cb2fe8ff5939976f0b22a58b06fa47ffb656a0740fdb41d25ec79633874f7c9b` |
| UI | v0.1.6 | `481feb626f209522c5b31b73738916337ab9e5bf425c146bff249ff9049ea06e` |

새 Catalog·Checkout의 앱 소스는 `14b3fb3726f20780b7591155bae9154d90394899`이며 [앱 CI run 35067357831](https://github.com/AWSome-BIT-Bespin/retail-store-app/actions/runs/35067357831)에서 ECR/GAR 게시 검증이 통과했다. 이번 작업에서 앱 코드는 수정하지 않았다. 이전 로컬 UI v0.1.7 후보는 게시하지 않는다.

## 검증 결과

| 검증 | 실제 결과 |
|---|---|
| 로컬 CD 회귀 테스트 | 56개 통과 |
| AWS 실제 입력 검사, AWS/GCP fixture 구조 검사 | 통과. GCP fixture는 실제 GKE 배포 증거가 아님 |
| Helm lint/render, 렌더링 타입 검사 | 통과 |
| Kubernetes server dry-run | 기존 리소스 27개 통과 |
| PR CI | [run 35070911633](https://github.com/AWSome-BIT-Bespin/retail-store-gitops/actions/runs/35070911633), Helm·Argo bootstrap 작업 통과 |
| Argo CD | 리소스 27개 Synced, Application Healthy |
| 서비스 health | Catalog OK, Checkout ok, Cart/Orders/UI UP |
| ALB UI | 홈·상품·장바구니·배송·주문 폼 HTTP 200 |
| 데이터 흐름 | 가상 회원가입 → 상품 1개 추가 → 배송 선택 → 데모 주문 → 내 주문 조회 통과 |
| 새 로그인 세션 | 새 cookie jar로 로그인 후 같은 주문 ID 조회 통과 |

최종 주문 ID: `aa197393-db53-4de1-8c86-960c0d0eba57`, 시험 시간: **17:01:46–17:01:48 KST**. 가상 고객은 `cicd-20260916-248c5957f1@example.com`이며 실제 개인정보·결제 정보는 사용하지 않았다. 앱의 주문 처리 소스에 외부 결제 호출이 없음을 확인했다.

첫 시험도 주문 생성·조회까지 성공했으나 검증 도구가 주문 목록에 `/logout` 폼이 있다고 잘못 가정해 후속 단계에서 멈췄다. 앱을 수정하지 않고 시험 도구를 새 세션 로그인 방식으로 바로잡아 재검증했다. 첫 주문 ID는 `c56e8a45-5f7e-4df2-8be4-3e3ca6ee4017`이다. 가상 계정·주문 각 2개가 시험 데이터로 남아 있다. 비밀번호는 로그·파일에 저장하지 않았다.

## 복구 준비와 남은 범위

- 정상 인수 기준: `fa895c3dc1c1651102de24a91e4785d1b84eebe2`. 환경/Application 파일을 유지하고 이미지 버전만 되돌리는 방식으로 복구한다.
- Bastion 복구 파일: `/home/ec2-user/retail-cd-20260916-gsa7ufke/rollback-resources.json`. 리소스 27개이며 DB 데이터와 외부 Secret 비밀값 백업은 아니다.
- **실제 버전 하향 롤백 훈련은 미실시**다. DB 호환성과 담당자 작업 시간을 확인해 별도 훈련하고 시간을 측정한다.
- WhaTap 환경변수·기동은 확인했다. 로그인된 프로젝트 목록에서 `retail-store`(50987)를 열었으나 상세 화면이 로딩 후 오류를 표시했다. 이 프로젝트와 현재 EKS의 대응 및 실제 애플리케이션 수집·요청 추적은 미확인이다. 이 화면 오류만으로 수집 장애를 단정하지 않는다.
- GCP는 이미지 게시와 차트 검사까지만 확인했다. GKE 배포, 데이터 복제, 장애 전환, RTO/RPO 측정은 별도 진행 대상이다.

수동 sync에 사용한 SSM command: 기준 인수 `d0625797-5900-4094-a07a-df11a4d6d314`, 이미지 승격 `aafd2140-82ee-4a8f-8700-37389e09c597`, digest/health 확인 `b9bbacce-e457-4920-895c-9e19e1d1ffbe`.
