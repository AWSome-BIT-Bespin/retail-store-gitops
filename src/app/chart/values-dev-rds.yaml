# Legacy, incomplete environment file. See environments/aws/values.example.yaml
# and runbooks/cd-inputs.md. Empty inputs are not deployment-ready.
cart:
  app:
    persistence:
      provider: dynamodb
      dynamodb:
        tableName: retail-store-cart
        createTable: false
  dynamodb:
    create: false

  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: ""

catalog:
  app:
    persistence:
      provider: in-memory

checkout:
  app:
    persistence:
      provider: redis
      redis:
        endpoint: ""
        tls: false
    endpoints:
      orders: http://orders:80
  redis:
    create: false

  whatap:
    enabled: false

orders:
  app:
    persistence:
      provider: postgres
      endpoint: ""
      database: orders
      secret:
        create: false
        name: orders-db

    messaging:
      provider: in-memory
      rabbitmq:
        secret:
          create: false

  postgresql:
    create: false

  rabbitmq:
    create: false

ui:
  service:
    type: ClusterIP
  ingress:
    enabled: true
    className: alb
  app:
    session:
      redis:
        endpoint: retailstore-valkey-001.lk6esl.0001.apn2.cache.amazonaws.com:6379
        tls: false
