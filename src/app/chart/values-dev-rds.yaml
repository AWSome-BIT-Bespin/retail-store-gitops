cart:
  app:
    persistence:
      provider: dynamodb
      dynamodb:
        tableName: retail-test-cart
        createTable: false
  dynamodb:
    create: false

  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::350606136784:role/RetailTestCartDynamoDBRole


catalog:
  app:
    persistence:
      provider: in-memory



checkout:
  app:
    persistence:
      provider: redis
    endpoints:
      orders: http://orders:80
  redis:
    create: false

  whatap:
    enabled: false

orders:
  app:
    persistence:
      provider: postgres
      endpoint: "retail-test-orders.cmnrhpznom2o.ap-northeast-2.rds.amazonaws.com:5432"
      database: orders
      secret:
        create: false
        name: orders-db

    messaging:
      provider: in-memory
      rabbitmq:
        secret:
          create: false


  postgresql:
    create: false

  rabbitmq:
    create: false

ui:
  service:
    type: ClusterIP
  ingress:
    enabled: true
    className: alb
  app:
    session:
      redis:
        endpoint: retail-test-redis.lk6esl.ng.0001.apn2.cache.amazonaws.com:6379